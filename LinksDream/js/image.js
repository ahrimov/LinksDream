/*const imageSize = 50;
function drawImage(imageSource, x, y){
	var image = new Image();
	image.src = imageSource;
	image.onload = function(x, y){
		ctx.drawImage(image, x, y, imageSize, imageSize);
	}
}*/